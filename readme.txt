run the database_init.js file first to initialise the database and the tables within the database.
Then provide any password if required in the password field of mysql.createConnection in the database_connection.js file
Then run both the frontend and backend.